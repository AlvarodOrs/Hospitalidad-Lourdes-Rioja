type Props = {
    theme: string;
    data: {
        parentPage: string;
        separator: string;
        page: string;
        eyebrow: string;
        title: string;
        enfasis: string;
        paragraph: string;
        image: string;
        alt: string;
    };
};

export default function HighlightsDates({ theme, data }: Props ) {
  return (
    <>
    </>
  )
}